# Patch Plans

Focused patch plans created by the review → patch cycle. Each plan targets a specific review issue with minimal, surgical changes.

Generated by `adw_review.py` when blocker issues are found, or by `adw_patch.py` when triggered by `adw_patch` keyword.
